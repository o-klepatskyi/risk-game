package gui.multiplayer_menu;

import javax.swing.*;

class FillerButton extends JButton {
    public FillerButton(int width, int height) {
        setSize(width, height);
        setVisible(false);
    }
}
